# OpenLive 测试

## 目标
验证 `OpenLiveProvider` 在官方身份码模式下的基础可用性与降级提示。

## 用例
- 输入合法身份码并连接
- 缺失凭证时显示可理解错误
- 心跳持续发送
- 长连接断开后自动重连
- 房间结束时正确清理会话

## 观察项
- `/start /heartbeat /end` 生命周期是否按预期触发
- 事件映射后消息是否进入统一消息模型
- token / game_id 生命周期是否正确
- 连接失败时是否写入 Crash Report

## 通过标准
- 连接过程可预期
- 错误提示清晰
- 异常不会导致整个应用失效
