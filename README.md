# LiveDanmu

一个基于 `Tauri 2 + Vue 3 + TypeScript + Vite + Element Plus + Pinia` 的跨平台哔哩哔哩直播弹幕桌面客户端。

## 开发

```bash
npm install
npm run tauri dev
```

## 打包

```bash
npm run tauri build
```

默认已配置：
- macOS `DMG`
- Windows `NSIS EXE`
